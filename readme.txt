1. In terminal, "cd backend; npm install; node initializedb.js; node server.js"
2. Open new terminal, "cd frontend; npm install; npm start"
3. if want end the server, "npx kill-port 5001; npx kill-port 3000"