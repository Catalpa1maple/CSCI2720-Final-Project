1. First in mongodb create a database named "cs2720" and collection named "loginsystem"
2. In terminal, "cd backend && npm install && node initializedb.js && node server.js"
3. Open new terminal, "cd frontend && npm install && npm start"
4. if want end the server, "npx kill-port 5001 && npx kill-port 3000"